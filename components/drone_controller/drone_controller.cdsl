import "CameraRGBDSimple.idsl";
import "JoystickAdapter.idsl";
import "CoppeliaUtils.idsl";

Component drone_controller
{
    Communications
    {
        implements CoppeliaUtils;
        requires CameraRGBDSimple;
        publishes JoystickAdapter;
    };
    language python;
    gui Qt(QMainWindow);
    statemachine "statemachine.smdsl";

};

